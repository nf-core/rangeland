nextflow_pipeline {

    name "Test pipeline with non-default parameters"
    script "../main.nf"

    test("Params: --save_ard true --save_tsa true --sensors_level2 'LND05' --indexes 'RED NIR SWIR2' --end_date '1988-12-31'") {

        when {
            params {
                outdir         = "$outputDir"
                save_ard       = true
                save_tsa       = true
                sensors_level2 = "LND05"
                indexes        = "RED NIR SWIR2"
                end_date       = "1988-12-31"
            }
        }

        then {
            // stable_name: All files + folders in ${params.outdir}/ with a stable name
            def stable_name = getAllFilesFromDir(params.outdir, relative: true, includeDir: true, ignore: ['pipeline_info/*.{html,json,txt}'])
            assertAll(
                { assert workflow.success},
                { assert snapshot(
                    // Number of successful tasks
                    workflow.trace.succeeded().size(),
                    // pipeline versions.yml file for multiqc from which Nextflow version is removed because we test pipelines on multiple Nextflow versions
                    removeNextflowVersion("$outputDir/pipeline_info/nf_core_rangeland_software_mqc_versions.yml"),
                    // All stable path name, with a relative path
                    stable_name
                ).match() }
            )

        }

    }

}
